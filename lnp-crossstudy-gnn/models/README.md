# models/

This folder holds model architecture definitions and training scripts. The main entry points are the GNN (graph neural network with atom-level attribution), a random forest baseline, an XGBoost baseline, and a simple MLP. Each model is written to accept the same featurized input format produced by the preprocessing pipeline.
