# preprocessing/

Scripts for everything that happens before model training: SMILES parsing and validation with RDKit, molecular fingerprint generation, graph featurization for the GNN, dataset harmonization across studies, and leave-one-study-out split construction. Run these in order before touching anything in `models/`.
