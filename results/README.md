# results/

Outputs from trained models land here: metric tables (R², RMSE, MAE per study and overall), prediction scatter plots, GNN atom-level attribution maps, and any saved model checkpoints. This folder is excluded from version control for large binary files — regenerate outputs by running the training scripts.
